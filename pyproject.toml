[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "risk-aware-convoy-escort-allocation-milp"
version = "0.3.0"
description = "Synthetic deterministic, stochastic, and robust MILP models for risk-aware convoy escort allocation."
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
keywords = ["operations-research", "milp", "optimization", "resource-allocation", "stochastic-programming", "robust-optimization", "pulp"]
dependencies = [
  "pulp>=2.8,<4",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
]

[project.scripts]
convoy-allocation = "convoy_allocation.cli:main"
convoy-allocation-uncertain = "convoy_allocation.uncertainty_cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
